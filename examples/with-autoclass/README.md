# Autoclass Cost Optimisation

GCS bucket with Autoclass enabled (terminal class: NEARLINE) to automatically move objects to the optimal storage tier based on access patterns.

## Usage

```bash
terraform init -backend=false
terraform validate
```
