# Nearline Cold Storage

GCS bucket using Nearline storage class with a 90-day delete lifecycle rule for infrequently accessed data.

## Usage

```bash
terraform init -backend=false
terraform validate
```
