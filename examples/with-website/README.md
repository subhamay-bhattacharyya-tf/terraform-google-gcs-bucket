# Static Website Hosting

GCS bucket configured for static website hosting with index/404 pages and multi-origin CORS rules covering both GET and POST methods.

## Usage

```bash
terraform init -backend=false
terraform validate
```
