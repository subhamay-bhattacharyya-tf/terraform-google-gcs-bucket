# All Features Enabled

GCS bucket exercising all supported features: versioning, website hosting, CORS, lifecycle delete rules, CMEK encryption, and comprehensive labels.

## Usage

```bash
terraform init -backend=false
terraform validate
```
