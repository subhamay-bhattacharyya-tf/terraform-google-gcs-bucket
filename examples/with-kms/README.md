# CMEK Encryption

GCS bucket encrypted with a customer-managed KMS key (CMEK). Replace the `kms_key_name` placeholder with a real KMS key resource path before applying.

## Usage

```bash
terraform init -backend=false
terraform validate
```
