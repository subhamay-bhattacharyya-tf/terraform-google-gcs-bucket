# CORS for Web Use

GCS bucket configured with a single-origin CORS rule and static website settings for browser-accessible assets.

## Usage

```bash
terraform init -backend=false
terraform validate
```
